[project]
name = "api-requests"
version = "0.1.0"
description = "職業訓練用 Python Requests API 串接教材"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.2",
    "requests>=2.32",
]

[tool.uv]
package = false
