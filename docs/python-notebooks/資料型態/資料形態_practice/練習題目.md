### Problem. 確認資料型態

題目描述：請建立三個變數，分別儲存學生的分數、身高與姓名，並印出它們的資料型態。

變數名稱與輸入設定：

- score：數值設定為 88
- height：數值設定為 175.5
- student_name：數值設定為 "小明"
- 輸出要求：依序使用 type() 函式印出這三個變數的資料型態。

### Problem. 數值運算與型態自動轉換

請計算買水果的總花費，並觀察 Python 如何自動處理資料型態的轉換。

變數名稱與輸入設定：

- apple_count：數值設定為 8
- price_per_apple：數值設定為 15.5
- total_price：設定為 apple_count 乘以 price_per_apple

輸出要求：

- 第一行印出 total_price
- 第二行印出 total_price 的資料型態

### Problem. 訂單明細

請建立：

```
product_name
price
count
```

並輸出：

```
商品名稱：RTX 5090
商品單價：89990
購買數量：2
總金額：179980
```

要求：

- 總金額需自行計算
- 使用 type() 檢查總金額型態

### Problem. 遊戲角色資訊面板

請建立：

```
player_name
job
level
hp
mp
```

並輸出：

```
====================
角色資訊

名稱：Shadow
職業：刺客
等級：35
HP：1500
MP：320
====================
```

### Problem. 製作終端機 Loading 畫面

請建立：

```
loading_text = "Loading"
```

並輸出：

```
Loading..........
```

要求：

- 點點需使用字串乘法產生
- 不可直接手打很多個 .

### Problem. 字串轉數字的強制轉換

模擬接收到字串格式的資料，請將其轉換成正確的數值型態後進行數學計算。

變數名稱與輸入設定：

- id_str：數值設定為 "1234" (字串型態)
- base_number：數值設定為 500
- final_password：將 id_str 轉為整數型態後，加上 base_number

輸出要求：印出 final_password 的數值。

### Problem. 數字轉字串的串接

請將數字強制轉換為字串，以便與其他文字串接成一個完整的句子。

變數名稱與輸入設定：

- temperature：數值設定為 25.5
- weather_msg：將 temperature 轉為字串後，與前綴字串 "今天的氣溫是 " 以及後綴字串 " 度" 進行加法串接

輸出要求：印出 weather_msg 的完整內容。

### Problem. 字串的乘法與加法

請利用字串乘法來重複特定符號，並結合字串加法設計一個分隔線標語。

變數名稱與輸入設定：

- star：數值設定為 "\*"
- word：數值設定為 " 歡迎光臨 "
- banner：由 10 個 star，加上 word，再接上 10 個 star 組合而成

輸出要求：印出 banner 的完整內容。

### Problem. 系統警告訊息

請建立：

```
warning_message
```

並輸出：

```
🚨🚨🚨 系統異常 🚨🚨🚨
```

要求：

- 使用字串乘法
- 中間訊息需來自變數

### Problem. 製作密碼遮罩

請建立：

```
password = "python123456"
```

並輸出：

```
************
```

要求：

- 不可直接輸入很多個 \*
- 必須根據密碼長度自動生成

### Problem. 路徑表示法

請輸出以下 Windows 路徑：

```
C:\Users\Allen\Desktop\Python\project
```

要求：

- 使用 raw string r""
- 再用一般字串寫一次

### Problem. 模擬資料封包傳輸

請建立：

```
message = "使用者登入成功"
```

並完成：

- encode 成 utf-8
- 印出 bytes 結果
- decode 回原始字串

輸出格式：

```
原始資料：
打包後：
拆包後：
```

### Problem. 資料的打包與拆包

請將中文字串打包成二進位資料 (bytes)，觀察其長度變化，再將其拆包轉回正常字串。

變數名稱與輸入設定：

- secret_msg：數值設定為 "機密任務"
- encoded_msg：將 secret_msg 以 utf-8 編碼格式進行打包 (encode)
- decoded_msg：將 encoded_msg 解碼 (decode) 轉回字串

輸出要求：

- 第一行使用 len() 函式印出 encoded_msg 的位元組長度
- 第二行印出 decoded_msg 的內容
